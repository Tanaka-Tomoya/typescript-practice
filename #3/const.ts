const wideningZero = 0;
const nonWideningZero = 0;
const asNonWideningZero = 0 as 0;

let ZeroA = 0; // number
let ZeroB = wideningZero; // number
let ZeroC = nonWideningZero; // 0
let ZeroD = asNonWideningZero; // 0
