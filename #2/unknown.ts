const certainlyNumbers: number[] = ['0'];
const maybeNumbers: any[] = ['0'];
const probablyNumbers: unknown[] = ['0'];
certainlyNumbers[0].toFixed(1);
maybeNumbers[0].toFixed(1);
//値の代入には寛容だが、利用に厳しい
probablyNumbers[0].toFixed(0);
