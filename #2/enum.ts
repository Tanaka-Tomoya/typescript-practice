enum Direction {
  Up,
  Down,
  Left,
  Right
}

const left = Direction.Left;

enum Ports {
  USER_SERVICE = '8080'
}

enum Ports {
  REGISTER_SERVICE = '8081'
}

enum Ports {
  MEDIA_SERVICE = '8888'
}

//enumはマージされる(文字列型に限る)
